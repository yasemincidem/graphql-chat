type Post {
    text: String!
    created_at: String!
    from: User!
    to: User
}
input PostInput {
    to: String!
    from: String!
    text: String!
}
extend type Mutation {
    sendMessage(input: PostInput!): [Post!]
}
type Subscription {
    newMessage: Post!,
}
